{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "python -m pip install -r requirements.txt",
      "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn locallibrary.wsgi:application"
    }
  }
  